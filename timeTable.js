function timeTable(number){
    
    for (i = 1 ; i <= 10 ; i++){
        const calculaton = i*number;
        const result = i + "*" + number;
        const filnalResult = result + "=" + calculaton;
        outputResut = filnalResult
        console.log(filnalResult)
        
    }
    
}
const inputNumber = timeTable(2);
